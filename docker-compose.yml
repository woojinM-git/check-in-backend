services:
  mysql80:
    image: mysql:8.0
    container_name: mysql80
    networks:
      - checkin_net
    volumes:
      - ./v1.sql:/docker-entrypoint-initdb.d/v1.sql
      - mysql_data:/var/lib/mysql
    env_file:
      - .env
    environment:
      MYSQL_ROOT_PASSWORD: "${DB_PASSWORD}"
      MYSQL_DATABASE: "checkin"
      MYSQL_USER: "${DB_USERNAME}"
      MYSQL_PASSWORD: "${DB_PASSWORD}"
      TZ: "Asia/Seoul"
    ports:
      - "4444:3306"
    restart: always

  redis:
    image: redis:7.2
    container_name: redis
    networks:
      - checkin_net
    environment:
      TZ: "Asia/Seoul"
    ports:
      - "6379:6379"
    restart: always

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backend
    networks:
      - checkin_net
    ports:
      - "8888:8888"
    depends_on:
      - mysql80
      - redis
    env_file:
      - .env
    environment:
      TZ: "Asia/Seoul"
      SPRING_PROFILES_ACTIVE: "prod"
      # Database
      DB_URL: "jdbc:mysql://mysql80:3306/checkin?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci"
      DB_USERNAME: "${DB_USERNAME}"
      DB_PASSWORD: "${DB_PASSWORD}"
      # Redis
      SPRING_DATA_REDIS_HOST: "redis"
      SPRING_DATA_REDIS_PORT: "6379"
      # JPA
      SPRING_JPA_HIBERNATE_DDL_AUTO: "validate"
      SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT: "org.hibernate.dialect.MySQL8Dialect"
      # JWT
      JWT_SECRET: "${JWT_SECRET}"
      # Gmail
      GMAIL_USERNAME: "${GMAIL_USERNAME}"
      GMAIL_APP_PASSWORD: "${GMAIL_APP_PASSWORD}"
      # Toss Payments
      TOSS_SECRET_KEY: "${TOSS_SECRET_KEY}"
      TOSS_CLIENT_KEY: "${TOSS_CLIENT_KEY}"
      # AWS S3
      AWS_S3_ACCESS_KEY: "${AWS_S3_ACCESS_KEY}"
      AWS_S3_SECRET_ACCESS_KEY: "${AWS_S3_SECRET_ACCESS_KEY}"
      AWS_S3_BUCKET_NAME: "${AWS_S3_BUCKET_NAME}"
      AWS_S3_REGION: "${AWS_S3_REGION}"
      # API URL
      NEXT_PUBLIC_API_URL: "${NEXT_PUBLIC_API_URL}"
      # CORS
      CORS_ALLOWED_ORIGINS: "${CORS_ALLOWED_ORIGINS}"
    restart: always

networks:
  checkin_net:
    driver: bridge

volumes:
  mysql_data:

